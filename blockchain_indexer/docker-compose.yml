version: "3.8"

services:
  indexer-db:
    image: postgres:16-alpine
    container_name: almizan-indexer-db
    environment:
      POSTGRES_USER: ponder
      POSTGRES_PASSWORD: ponderpassword12xY12
      POSTGRES_DB: ponder
    ports:
      - "5433:5432"
    volumes:
      - indexer_pgdata:/var/lib/postgresql/data
    mem_limit: 256m

  indexer-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: almizan-indexer-app
    depends_on:
      - indexer-db
    environment:
      # Ponder database connection (PostgreSQL)
      DATABASE_URL: postgresql://ponder:ponderpassword@indexer-db:5433/ponder
      DATABSE_SCHEMA: public

      # Chain RPC URLs (set these in your deployment env)
      PONDER_RPC_URL_1: ${PONDER_RPC_URL_1}
      PONDER_RPC_URL_42: ${PONDER_RPC_URL_42}

      NODE_ENV: production
    ports:
      - "42069:42069"
    restart: unless-stopped
    mem_limit: 350m

volumes:
  indexer_pgdata:
